version: "3.3"
services:
  development:
    build:
      context: . 
      dockerfile: ../dockerfiles/dev.base.dockerfile
    container_name: "development"
    environment:
      - USER=$USER
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix