- defaults:
            link:
                create: true
                relink: true

- clean: ['~', '~/.config']

- link:
        ~/.dotfiles: ''
        ~/.zshrc: 
        ~/.vimrc: 
        ~/.gitignore_global:
        ~/.gitconfig:
