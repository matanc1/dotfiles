- defaults:
            link:
                create: true
                relink: true

- clean: ['~', '~/.config']

- link:
        ~/.dotfiles: ''
        ~/.zshrc: 
        ~/.bashrc:
        ~/.vimrc: 
        ~/.gitignore_global:
        ~/.gitconfig:

- shell:
      - [git submodule update --init --recursive, Installing submodules]
